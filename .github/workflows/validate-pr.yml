name: Validate Pull Request

on:
  pull_request:
    branches: [master, main]
    paths:
      - '**.html'
      - '**.css'
      - '**.js'
      - '.github/workflows/**'

jobs:
  validate:
    name: Code Quality & Structure Validation
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check CSS build is in sync
        run: |
          echo "🧩 Verifying styles.css matches css/ modules..."
          npm run build:css:check

      - name: Validate HTML
        run: |
          echo "🔍 Validating HTML structure..."
          npx html-validate index.html --formatter stylish || echo "⚠️ HTML validation warnings found"
        continue-on-error: true

      - name: Lint CSS
        run: |
          echo "🎨 Linting CSS modules..."
          npx stylelint "css/*.css" --config-basedir . || echo "⚠️ CSS linting warnings found"
        continue-on-error: true

      - name: Lint JavaScript
        run: |
          echo "📜 Linting JavaScript..."
          npx eslint "*.js" --fix-dry-run || echo "⚠️ JavaScript linting warnings found"
        continue-on-error: true

      - name: Check file sizes
        run: |
          echo "📊 Checking file sizes..."
          echo "=== File Sizes ==="
          ls -lh *.html *.css *.js 2>/dev/null || true

          echo ""
          echo "=== Size Limits Check ==="

          # Check if any HTML file is larger than 500KB
          if find . -maxdepth 1 -name "*.html" -size +500k | grep -q .; then
            echo "⚠️ Warning: HTML files larger than 500KB found"
            find . -maxdepth 1 -name "*.html" -size +500k -exec ls -lh {} \;
          else
            echo "✅ HTML file sizes OK"
          fi

          # Check if any CSS file is larger than 100KB
          if find . -maxdepth 1 -name "*.css" -size +100k | grep -q .; then
            echo "⚠️ Warning: CSS files larger than 100KB found"
            find . -maxdepth 1 -name "*.css" -size +100k -exec ls -lh {} \;
          else
            echo "✅ CSS file sizes OK"
          fi

          # Check if any JS file is larger than 50KB
          if find . -maxdepth 1 -name "*.js" -size +50k | grep -q .; then
            echo "⚠️ Warning: JavaScript files larger than 50KB found"
            find . -maxdepth 1 -name "*.js" -size +50k -exec ls -lh {} \;
          else
            echo "✅ JavaScript file sizes OK"
          fi

      - name: Check required files
        run: |
          echo "📋 Checking required files..."

          REQUIRED_FILES=("index.html" "styles.css" "script.js")
          MISSING_FILES=()

          for file in "${REQUIRED_FILES[@]}"; do
            if [ ! -f "$file" ]; then
              MISSING_FILES+=("$file")
              echo "❌ Missing required file: $file"
            else
              echo "✅ Found: $file"
            fi
          done

          if [ ${#MISSING_FILES[@]} -gt 0 ]; then
            echo ""
            echo "⚠️ Missing ${#MISSING_FILES[@]} required file(s)"
            exit 1
          fi

          echo ""
          echo "✅ All required files present"

      - name: Check HTML structure
        run: |
          echo "🏗️ Checking HTML structure..."

          # Check for required meta tags
          echo "Checking meta tags..."
          grep -q 'charset=' index.html && echo "✅ Charset meta tag found" || echo "❌ Missing charset meta tag"
          grep -q 'viewport' index.html && echo "✅ Viewport meta tag found" || echo "❌ Missing viewport meta tag"
          grep -q 'description' index.html && echo "✅ Description meta tag found" || echo "❌ Missing description meta tag"

          # Check for external resources
          echo ""
          echo "Checking external resources..."
          grep -q 'link.*stylesheet.*styles.css' index.html && echo "✅ External CSS linked correctly" || echo "❌ External CSS not linked"
          grep -q 'script.*src.*script.js' index.html && echo "✅ External JS linked correctly" || echo "❌ External JS not linked"

          # Check for inline styles/scripts (should be minimal)
          echo ""
          echo "Checking for inline styles/scripts..."
          INLINE_STYLES=$(grep -c '<style>' index.html || echo "0")
          INLINE_SCRIPTS=$(grep -c '<script>.*[^src=]' index.html || echo "0")

          if [ "$INLINE_STYLES" -gt 0 ]; then
            echo "⚠️ Found $INLINE_STYLES inline style tag(s) - consider moving to styles.css"
          else
            echo "✅ No inline styles found"
          fi

          if [ "$INLINE_SCRIPTS" -gt 1 ]; then
            echo "⚠️ Found $INLINE_SCRIPTS inline script tag(s) - consider moving to script.js"
          else
            echo "✅ Minimal inline scripts"
          fi

      - name: Check JavaScript functionality
        run: |
          echo "🧪 Checking JavaScript syntax..."

          # Check for common JavaScript errors
          if node -c script.js 2>/dev/null; then
            echo "✅ JavaScript syntax is valid"
          else
            echo "❌ JavaScript syntax errors found"
            node -c script.js
            exit 1
          fi

          # Check for console.log statements (should be removed in production)
          echo ""
          echo "Checking for debug statements..."
          if grep -n "console\.log\|console\.debug\|debugger" script.js; then
            echo "⚠️ Found debug statements - consider removing for production"
          else
            echo "✅ No debug statements found"
          fi

      - name: Check CSS quality
        run: |
          echo "🎨 Checking CSS quality..."

          # Check for !important overuse
          echo "Checking for !important usage..."
          IMPORTANT_COUNT=$(grep -c '!important' styles.css || echo "0")

          if [ "$IMPORTANT_COUNT" -gt 10 ]; then
            echo "⚠️ Found $IMPORTANT_COUNT !important declarations - consider refactoring"
            grep -n '!important' styles.css | head -10
          elif [ "$IMPORTANT_COUNT" -gt 0 ]; then
            echo "ℹ️ Found $IMPORTANT_COUNT !important declarations"
          else
            echo "✅ No !important declarations"
          fi

          # Check for vendor prefixes (could use autoprefixer)
          echo ""
          echo "Checking for vendor prefixes..."
          PREFIXES=$(grep -c '\-webkit-\|\-moz-\|\-ms-\|\-o-' styles.css || echo "0")

          if [ "$PREFIXES" -gt 0 ]; then
            echo "ℹ️ Found $PREFIXES vendor prefixes - consider using autoprefixer"
          else
            echo "✅ No manual vendor prefixes (or already processed)"
          fi

      - name: Security check
        run: |
          echo "🔒 Running security checks..."

          # Check for inline event handlers (XSS risk)
          echo "Checking for inline event handlers..."
          if grep -E 'on(click|load|error|mouse|key)=' index.html; then
            echo "⚠️ Found inline event handlers - consider moving to script.js for better CSP"
          else
            echo "✅ No inline event handlers found"
          fi

          # Check for external script sources
          echo ""
          echo "Checking external resources..."
          if grep -o 'src="http[^"]*"' index.html | grep -v 'fonts.googleapis.com\|fonts.gstatic.com'; then
            echo "ℹ️ External resources found - ensure they are from trusted sources"
          else
            echo "✅ Only trusted external resources"
          fi

      - name: Generate report summary
        if: always()
        run: |
          echo ""
          echo "================================================"
          echo "📊 VALIDATION SUMMARY"
          echo "================================================"
          echo ""
          echo "Repository: ${{ github.repository }}"
          echo "PR: #${{ github.event.pull_request.number }}"
          echo "Branch: ${{ github.head_ref }}"
          echo "Commit: ${{ github.sha }}"
          echo ""
          echo "✅ Validation completed!"
          echo ""
          echo "Review the checks above for any warnings or issues."
          echo "================================================"

      - name: Comment on PR
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            // Get file sizes
            const getFileSize = (file) => {
              try {
                const stats = fs.statSync(file);
                return (stats.size / 1024).toFixed(2) + ' KB';
              } catch {
                return 'N/A';
              }
            };

            const htmlSize = getFileSize('index.html');
            const cssSize = getFileSize('styles.css');
            const jsSize = getFileSize('script.js');

            const comment = `## 🤖 Automated Code Validation Report

            **PR:** #${{ github.event.pull_request.number }}
            **Branch:** \`${{ github.head_ref }}\`
            **Commit:** \`${{ github.sha }}\`

            ### 📊 File Sizes
            | File | Size |
            |------|------|
            | index.html | ${htmlSize} |
            | styles.css | ${cssSize} |
            | script.js | ${jsSize} |

            ### ✅ Validation Checks
            - ✅ HTML structure validation
            - ✅ CSS linting
            - ✅ JavaScript linting
            - ✅ File size checks
            - ✅ Required files present
            - ✅ Security checks

            ### 📝 Notes
            See the [Actions tab](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for detailed results.

            ---
            *This is an automated validation report. Review any warnings above.*
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
